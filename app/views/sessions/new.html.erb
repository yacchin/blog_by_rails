<div id="nav" class="text-center">

	<h1 style="margin-top: 30px;margin-bottom:40px">Sign In</h1>

	<%= form_tag "/sessions/create" do %>
	  <!-- name -->
	  <div class="control-group">
	  	<%= label :user, :name, :style => "margin-left:-180px" %>
	  	<div class="controls">
	  		<%= text_field :user, :name, :class => 'text_field' %>
	  	</div>
	  </div>

	  <!-- password -->
	  <div class="control-group">
	  	<%= label :user, :password, :style => "margin-left:-155px" %>
	  	<div class="controls">
	  		<%= password_field :user, :password, :class => 'text_field' %>
	  	</div>
	  </div>

      <%= form_tag({:action => 'create'},{:method => "post"}) do  %>
        <%= submit_tag 'Sign in', :class => 'btn btn-primary', :style => "margin-top:20px" %>
      <% end %>

	<% end %>

</div>